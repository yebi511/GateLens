# ADR 0003：MVP 采用只读旁路部署

- 状态：Accepted
- 日期：2026-07-24

## 背景

网关路径是高风险生产路径。将首版放入代理或配置写入链路会扩大故障面和权限需求。

## 决策

MVP 只用 Kubernetes `get/list/watch` 和外部观测后端查询权限读取数据，不修改资源、不注入流量、不代理请求。

## 后果

首版无法自动修复或捕获全部私有运行时决策，但部署和安全审查更简单，且不会成为业务请求单点。
