# ADR 0002：区分声明、解析、观测和推断

- 状态：Accepted
- 日期：2026-07-24

## 背景

静态配置、控制面最终状态和数据面实际请求可能不一致。若不说明来源，用户会将模拟误认为真实流量。

## 决策

所有数据和结论强制标记 `Declared`、`Resolved`、`Observed` 或 `Inferred`，并提供来源、观察时间、关联方式和规则版本。推断不作为运行事实展示。

## 后果

模型和 UI 增加元数据复杂度，但排障结论可审计，未知部分不会被掩盖。
